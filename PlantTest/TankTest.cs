using NUnit.Framework;
using Plant.Components;
using System;
using System.Collections.Generic;
using System.Text;

namespace PlantTest
{
    [TestFixture]
    public class TankTest
    {
        [Test]
        public void AddMaterial_Adding_MaterialAdded()
        {
           //Klinker k = new Klinker();
           // Tank t = new Tank(k);

           // Klinker newKlinker = new Klinker();
           // newKlinker.Add(45);

           // t.Add(newKlinker);

           // Assert.IsTrue(t.LevelQuantity == 45);
        }
    }
}
